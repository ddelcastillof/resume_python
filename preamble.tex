% !TEX TS-program = xelatex
% !TEX encoding = UTF-8 Unicode

\usepackage{babel}   % multi-language support
\usepackage{float}   % floats
\usepackage{url}     % urls
\usepackage{hyperref}
\usepackage{enumitem} % Customizing
\usepackage{marvosym} % Allows the use of symbols
\usepackage{parskip} % Spacing adjustments
\usepackage{ragged2e} % Justified text
\usepackage{geometry} % Required for adjusting page dimensions and margins
\usepackage{titlesec} % Custom section headers
\usepackage{booktabs} % For professional-looking tables
\usepackage{fontawesome5} % FontAwesome icons

% Main font

\usepackage{fontspec}

\usepackage{lastpage} % Required for \pageref{LastPage}

% Links

\usepackage{color}
\definecolor{linkcolour}{rgb}{0,0.2,0.6}

% Headers
  
% Format for \section
\titleformat{\section}                                             % Format for section
  {\normalfont\normalsize\bfseries\raggedright\uppercase}          % Format for the title: normal font, normal size, bold, right alignment, uppercase
  {\thesection}{1em}{}                                             % Label format and spacing

% Format for \subsection
\titleformat{\subsection}                                % Format for subsection
  {\normalfont\normalsize\bfseries\raggedright}          % Format: normal font, normal size, bold, right alignment
  {\thesubsection}{1em}{}                                % Label format and spacing

% Format for \subsubsection
\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\raggedright}     % Format: normal font, normal size, bold, right alignment
  {\thesubsubsection}{1em}{}                        % Label format and spacing
  
% Table centering
\let\oldtable\table
\let\endoldtable\endtable
\renewenvironment{table}[1][h!]
  {\oldtable[#1]\center}
  {\endcenter\endoldtable}